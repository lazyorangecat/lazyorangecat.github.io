@using _0524FirstMVC.Services
@using System.Collections.Generic
@using Microsoft.EntityFrameworkCore
@{
    MenuService menuService = new MenuService();
    List<Member> members = await menuService.GetAllMembers();

    var team1 = members.Where(x => x.Team == "第一組");
    var team2 = members.Where(x => x.Team == "第二組");
    var team3 = members.Where(x => x.Team == "第三組");
    var team4 = members.Where(x => x.Team == "第四組");

    var teamA = await menuService.GetTeamMembers("第一組");
    var teamB = await menuService.GetTeamMembers("第二組");
    var teamC = await menuService.GetTeamMembers("第三組");
    var teamD = await menuService.GetTeamMembers("第四組");


}
<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">_0524FirstMVC</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="members" asp-action="ListTable">ListTable</a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                        第一組
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var member in team1)
                        {
                            <a class="dropdown-item" href="~/Members/Details/@member.Id">@member.Name</a>

                        }
                        @*<a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>*@
                    </div>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                        第二組
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var member in team2)
                        {
                            <a class="dropdown-item" href="~/Members/Details/@member.Id">@member.Name</a>

                        }
                        @*<a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>*@
                    </div>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                        第三組
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var member in team3)
                        {
                            <a class="dropdown-item" href="~/Members/Details/@member.Id">@member.Name</a>

                        }
                        @*<a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>*@
                    </div>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                        第四組
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var member in team4)
                        {
                            <a class="dropdown-item" href="~/Members/Details/@member.Id">@member.Name</a>

                        }
                        @*<a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>*@
                    </div>
                </li>

            </ul>
            <form class="form-inline my-2 my-lg-0" action="~/Site/SearchMemberbyName" method="post">
                @*action="~/Site/SearchMemberbyName" method="post"*@
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="name">     @*name="name"*@
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>
